# vista-forge.github.io — per-repo memory index

One line per memory file. Content lives in the files, not here. Per the Increment
Protocol keep-test, this store holds **durable lessons only** — per-increment
journals belong in git history, not here.

- [org-logo-treatment](org-logo-treatment.md) — **The mark is the faceted-V art (2026-08-14), keyed on SATURATION, floating on black — no plate.** Source `v-forge-icon.png`; derived files, the exact key, and the `-depth 8` trap are in-file. ⚠️ **Do not port the old "never knock this out" rule forward** — it described the chromed-anvil render (still the org avatar), whose plate was a workaround for an unkeyable image.
- [stale-css-cache-not-a-code-bug](stale-css-cache-not-a-code-bug.md) — **"Section X doesn't show up" after a CSS-only push = stale browser CSS cache, not a code bug.** Pages caches assets ~10 min; hard-reload (Ctrl+Shift+R) fixes it. Prove it first: WebFetch the live CSS (external curl is sandbox-denied), then render the actual files in real Firefox headless — but the user's Firefox is already running, so you need `--new-instance --profile <tmpdir>`. Isolate a component with a tiny harness HTML that `<link>`s the real site.css.
- [⭐ The landing page is for the DECIDER](the-landing-page-is-for-the-decider.md) — 2026-09-20: three claims, each a figure + what it is/how it works/what it is worth; **every technical thing in ONE shut fold at the bottom**. ⚠️ "How does it work" is a technical detail — do not move facts up because they are impressive. Figures stay HTML (they reflow and cannot go stale); generated images were considered and declined.
- [site-content-is-unguarded](site-content-is-unguarded.md) — **Half the landing page is gated; know which half.** The `m`/`v` repo tables are generated from the registry (visibility included, so public-linking is mechanical) and red-gated by `make site-check`; the prose, the by-the-numbers figures and the non-registry tables are hand-written and **ungated — they rot silently** (2026-08-14: every figure low, some by half). Re-measure from the tree — method + the stale-`test-results.json` trap in-file — never copy the equally hand-written profile README. Two gate tiers, and why, in-file.
- [⚠️ two-deployments-one-chrome-ungated](two-deployments-one-chrome-ungated.md) — **This site and `forge-reference` are one visual product built by two toolchains, so the palette, the nav, the brand markup and the theme button are restated by hand in both — and NO gate compares them.** **+2026-09-19: the drift is now in ONE viewport** — the reference is FRAMED inside this site's Docs view, so both navs render at once; and the both-keys theme trick reaches only the SAME-ORIGIN copy (`localStorage` is per-origin) — so since 09-20 **Docs frames this site's own `/forge-docs/`** and the theme carries. ⚠️ **Corrected 09-20:** the two copies were NOT different vintages; the extra directories were 149 stale `.gz` corpses (`rsync --delete` spares `--exclude`d files). A file census answers a question about FILES, not content. Sharing the stylesheet is not an option (an off-host load fails that repo's `offline-check`). Both toggles must keep writing both theme keys (`theme` + `starlight-theme`), or one click leaves the two halves disagreeing. Includes the `justify-content:flex-end` scroll trap that hid the first nav item.
- [⚠️⚠️ a-tailnet-published-origin-cannot-be-embedded](a-tailnet-published-origin-cannot-be-embedded.md) — **A public page cannot embed a Funnel host from INSIDE the tailnet** — MagicDNS answers `100.x`, so the browser refuses silently (no request, no error), and the OPERATOR is the one reader who sees it fail. Not the server, not the code, not slowness — each checked. The discriminator: an unnavigated frame's `contentWindow.location` still reads.
- [public-claims-need-a-counterexample-check](public-claims-need-a-counterexample-check.md) — **Two live counter-examples defeat the obvious superlatives** (searched 2026-08-14): SSA's $1B AI-assisted COBOL modernization beats "first to AI"; DoD's MHS GENESIS (9.6M beneficiaries vs VA's 9.1M) beats "largest" on head count. Prefer checkable scale facts — and watch **attribution**: "VA's VistA is the first to…" credits the VA for vista-forge's work.
